{
  "name": "fvtt-module-portal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && tsc --noEmit && npm run build",
    "publish:release": "node scripts/publish-release.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "typescript": "5.9.3",
    "vite": "^8.3.0"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
